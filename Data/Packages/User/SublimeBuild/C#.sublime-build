{
     //"cmd": ["csc", "$file"],	// just compile
     "cmd": ["RunCSharp.bat", "$file"],	// Compile and run
     "file_regex": "^(...*?):([0-9]*):?([0-9]*)",
     "selector": "source.cs",
     "encoding": "cp936"
}